{
    // Because in this project, I used difference Python version between Ryu (Python 3.9) and Mininet-Wifi (Python 3.12)
    // If don't have this setting, code suggestion not work when editing topology file (Mininet) 
    "python.analysis.extraPaths": [
        "/usr/local/lib/python3.12/dist-packages"
    ],
    "python.autoComplete.extraPaths": [
        "/usr/local/lib/python3.12/dist-packages"
    ]
}